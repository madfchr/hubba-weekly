<h1>Hubba Weekly</h1>
<p><%= link_to 'New', new_email_path %></p>

<table>
  <% @emails.each do |email| %>
    <tr>
      <td><%= email.name %></td>
      <td><%= link_to 'Show', email_path(email) %></td>
      <td><%= link_to 'Edit', edit_email_path(email) %></td>
    </tr>
  <% end %>
</table>
